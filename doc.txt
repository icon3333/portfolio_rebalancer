# ISIN to Comprehensive Stock Information

This project demonstrates how to use yfinance to fetch comprehensive stock information directly using ISIN numbers, without needing to convert them to ticker symbols first.

## The Discovery

While yfinance is traditionally used with ticker symbols (e.g., "AAPL", "MSFT"), we've discovered that it can also handle International Securities Identification Numbers (ISINs) directly. This is an undocumented feature that works surprisingly well.

### Examples of Working ISINs:
- US88579Y1010 (3M Co)
- NL0000235190 (Airbus)
- DE000BASF111 (BASF)

## Technical Implementation

### 1. Project Setup

1. Create a new Python virtual environment:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

2. Install required packages:
```bash
pip install flask python-dotenv yfinance
```

3. Create the following project structure:
```
isin_ticker/
├── app.py
├── isin_logic.py
├── requirements.txt
└── templates/
    └── index.html
```

### 2. Core Implementation (isin_logic.py)

```python
from typing import Dict, Any
import logging
import yfinance as yf

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

def get_yfinance_info(isin: str) -> Dict[str, Any]:
    """
    Get detailed information about a stock using yfinance with ISIN
    
    Args:
        isin (str): ISIN of the stock
        
    Returns:
        Dict[str, Any]: Dictionary containing stock information
    """
    try:
        # Create a Ticker object using the ISIN
        ticker = yf.Ticker(isin)
        
        # Get basic info
        info = ticker.info
        
        return {
            'success': True,
            'data': {
                'ticker': info.get('symbol'),
                'currentPrice': info.get('currentPrice'),
                'marketCap': info.get('marketCap'),
                'dividendYield': info.get('dividendYield'),
                'peRatio': info.get('trailingPE'),
                '52WeekHigh': info.get('fiftyTwoWeekHigh'),
                '52WeekLow': info.get('fiftyTwoWeekLow'),
                'volume': info.get('volume'),
                'sector': info.get('sector'),
                'industry': info.get('industry'),
                'country': info.get('country'),
                'exchange': info.get('exchange'),
                'currency': info.get('currency'),
                'description': info.get('longBusinessSummary'),
                'website': info.get('website'),
                'previousClose': info.get('regularMarketPreviousClose'),
                'beta': info.get('beta'),
            }
        }
    except Exception as e:
        return {
            'success': False,
            'error': str(e)
        }
```

### 3. Flask Backend (app.py)

```python
from flask import Flask, render_template, request, jsonify
from isin_logic import get_yfinance_info
from dotenv import load_dotenv
import logging

# Set up logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/profile', methods=['POST'])
def get_profile():
    data = request.get_json() if request.is_json else request.form
    identifier = data.get('identifier', '').strip().upper()
    
    if not identifier:
        return jsonify({'error': 'No identifier provided'})
    
    try:
        result = get_yfinance_info(identifier)
        return jsonify(result)
    except Exception as e:
        logger.error(f"Error processing request: {str(e)}")
        return jsonify({'error': str(e)})

if __name__ == '__main__':
    app.run(debug=True)
```

### 4. Frontend Implementation (templates/index.html)

The frontend displays the stock information in an organized layout with the following sections:
- Basic Information (ticker, price, volume)
- Performance Metrics (52-week high/low, PE ratio, beta)
- Company Information (sector, industry, country)
- Company Description

Key HTML/JavaScript features:
- Uses Bulma CSS framework for styling
- Async/await for API calls
- Error handling and loading states
- Formatted number display (currency, percentages)

For the complete frontend code, please refer to the `templates/index.html` file in the repository.

### 5. Running the Application

1. Activate the virtual environment:
```bash
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

2. Start the Flask application:
```bash
python app.py
```

3. Open a web browser and navigate to:
```
http://127.0.0.1:5000
```

## Retrieved Information

Using this implementation, you can retrieve the following stock information using just an ISIN:

1. **Basic Information**
   - Ticker Symbol
   - Current Price
   - Previous Close
   - Market Cap
   - Volume

2. **Performance Metrics**
   - 52 Week High/Low
   - PE Ratio
   - Beta
   - Dividend Yield

3. **Company Information**
   - Sector
   - Industry
   - Country
   - Exchange
   - Currency
   - Website

4. **Additional Information**
   - Company Description

## Important Notes

1. **Rate Limiting**: Be mindful of yfinance's rate limits. Implement caching for production use.

2. **Data Accuracy**: Always validate information against official sources for critical applications.

3. **Market Hours**: Some information may be delayed or unavailable outside market hours.

4. **Future Compatibility**: Since using ISINs with yfinance is an undocumented feature, it may change in future versions.

5. **Error Handling**: The implementation includes robust error handling to manage cases where:
   - The ISIN is invalid
   - The stock information is unavailable
   - The yfinance API is unreachable

## Troubleshooting

1. **Missing Data**: Some fields might return 'N/A' if the information is not available for that particular stock.

2. **API Errors**: If you encounter frequent errors, consider:
   - Implementing exponential backoff
   - Adding request retries
   - Using a caching system

3. **Performance**: For better performance:
   - Cache responses
   - Implement rate limiting
   - Only request needed fields

## Contributing

Feel free to contribute to this project by:
1. Reporting issues
2. Suggesting enhancements
3. Creating pull requests

## License

This project is open-source and available under the MIT License.
